'use strict';

import React from 'react';

import core_customvar from '../core/core_customvar';

export default function body_customvar(props) {
    return (
        <div className="body">
            {core_customvar(props)}
        </div>
    );
}
