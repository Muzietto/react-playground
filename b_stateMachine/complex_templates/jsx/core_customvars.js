'use strict';

import React from 'react';

export default function core_type(props) {
    return (
        <div className="core">
            <p>This is the core customvars saying: {props.message}</p>
        </div>
    );
}
