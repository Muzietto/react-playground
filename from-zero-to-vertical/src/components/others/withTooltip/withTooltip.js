import React from 'react';
import Tooltip from '@material-ui/core/Tooltip';

const withTooltip = ({
  headerText = '',
  placement = 'top',
  // eslint-disable-next-line react/display-name
}) => Component => props => {

  return null;
  // you must return a Tooltip wrapping the Component through a <div>
};

export default withTooltip;
